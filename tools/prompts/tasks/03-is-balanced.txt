Write a Python function with exactly this signature:

    def is_balanced(text):

It takes a string and returns True if every round, square and curly bracket is closed in the correct order, and False otherwise. Characters that are not brackets are ignored. An empty string is balanced.

Use only the standard library. Output the function definition and nothing else: no explanation, no example, no markdown fences.
