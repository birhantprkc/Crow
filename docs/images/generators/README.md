# Generators

The scripts that render the images one directory up -- `_eq.py` and `_plots.py` (matplotlib), `_social.py` (Pillow; it reads the version out of `cli/crow.py`), `_og_dark.html` (the screenshot source). Run them from the repository root; each writes into `docs/images/`.
